#include "common_trig.h"

// Definitions are ONLY here
double cot(double z) {
    return 1.0 / tan(z);
}
double sec(double z) {
    return 1.0 / cos(z);
}
double csc(double z) {
    return 1.0 / sin(z);
}